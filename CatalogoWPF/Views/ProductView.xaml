<UserControl x:Class="CatalogoWPF.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CatalogoWPF.Views"
             xmlns:viewmodel="clr-namespace:CatalogoWPF.ViewModels"
             xmlns:properties="clr-namespace:CatalogoWPF.Properties"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewmodel:ProductViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{Binding Products}" Grid.Column="0"/>
        <Grid Grid.Column="1">
            <!--<Button Content="hola" Command="{Binding HolaCommand}"/>-->
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label Content="Nombre" Grid.Row="0"/>
            <TextBox Text="{Binding Name}" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
            <Label Content="Descripcion" Grid.Row="2"/>
            <TextBox Text="{Binding Description}" Grid.Row="3" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
            <Label Content="Precio" Grid.Row="4"/>
            <TextBox Text="{Binding Price}" Grid.Row="5" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
            <Button Content="Añadir" Grid.Row="6" HorizontalAlignment="Right" 
            Command="{Binding AddCommand}" VerticalAlignment="Bottom"/>
        </Grid>
    </Grid>
</UserControl>
