<UserControl x:Class="CatalogoWPF.Views.CategView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CatalogoWPF.Views"
             xmlns:viewmodel="clr-namespace:CatalogoWPF.ViewModels"
             xmlns:properties="clr-namespace:CatalogoWPF.Properties"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewmodel:CategViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
 
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{Binding Categories}" Grid.Column="0"/>
        <Grid Grid.Column="1">
            <!--<Button Content="hola" Command="{Binding HolaCommand}"/>-->
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>
            <Label Content="Nombre" Grid.Row="0"/>
            <TextBox Text="{Binding Name}" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Top"/> 
            <Button Content="Añadir" Grid.Row="2" HorizontalAlignment="Right" 
                    Command="{Binding AddCommand}" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
